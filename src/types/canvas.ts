export type Point = { x: number; y: number }
export type Size = { width: number; heigth: number }

export type Camera = { x: number; y: number; z: number }
export type Box = {
	minX: number
	minY: number
	maxX: number
	maxY: number
	width: number
	height: number
}
